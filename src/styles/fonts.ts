import { Inter, Lora, Poppins } from 'next/font/google';

// define your variable fonts
const inter = Inter();
const lora = Lora();
const poppins = Poppins({ weight: '400' });

export { inter, lora, poppins };
